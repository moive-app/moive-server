spring:
  application:
    name: MoiveBE
  profiles:
    include: local

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: 3600000
  refresh-token-expiration: 1209600000

google:
  maps:
    api-key: ${GOOGLE_MAPS_API_KEY}

kakao:
  transit:
    api-key: ${KAKAO_TRANSIT_API_KEY}
